import styled from "styled-components";

export const TodoRegisterWrapper= styled.div`
    display: flex;
    align-items: center;
    background-color: ${props => props.theme.boxBackgroundColor};
    height: 70px;
    border-radius: 20px; 
    margin-bottom: 15px;
`